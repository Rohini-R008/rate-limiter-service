events {}
http {
    upstream rate_limiter_app {
        server app1:8080;
        server app2:8080;
    }
    server {
        listen 8080;
        location / {
            proxy_pass http://rate_limiter_app;
            proxy_set_header Host $host;
        }
    }
}